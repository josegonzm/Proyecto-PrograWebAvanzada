@page
@model Interfaz.Pages.Usuarios.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1 class="text-3xl font-bold m-3"> Detalles de Usuario</h1>

<div class="m-4 flex items-center justify-center">
    <div class="bg-gray-200 p-6 rounded-lg shadow-md">
        <h4 class="text-lg font-semibold mb-4">Usuario</h4>
        <hr class="mb-4" />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Usuario.NombreUsuario)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Usuario.PasswordHash)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Usuario.Correo)
            </dt>
        </dl>
    </div>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Usuario?.Id">Editar</a> |
    <a asp-page="./Index">Volver al listado</a>
</div>
